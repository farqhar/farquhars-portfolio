

# Add teaser text to Chippy project card

Add a `teaser` property to the Chippy project object in `src/components/ProjectCells.tsx`:

```
teaser: "Characters born from real potato stamps, grown into a brand."
```

This matches the pattern used by Boondi and Analogue to Algorithm, giving the card a brief description beneath the title.

**File**: `src/components/ProjectCells.tsx`, line 34 — add `teaser` field after `title`.

