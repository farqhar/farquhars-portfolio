

# Fix Card 04 image — show full head with space above

## Change

In `src/components/TimelineCarousel.tsx`, line 57:
- Change `objectPosition: "center 20%"` → `"center 10%"` — moves the crop window higher in the image so the person's head is fully visible with breathing room above it.

